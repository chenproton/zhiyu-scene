# 部署说明

本项目支持一键部署，只需在项目根目录下执行：

```bash
./deploy.sh
```

即可自动完成构建和部署流程。

# 开发规范

1. 所有修改必须通过 `deploy.sh` 完成部署验证，确保脚本可以将所有代码变动一起更新，不手动远程服务器修改。
2. 每次完成一次问题处理/优化后，必须执行 git 提交并推送，保持代码仓库与线上环境一致。
3. 当前系统为演示系统，不需要考虑数据流转逻辑，只需要 mock 数据和交互流程达到我希望的效果就行。
